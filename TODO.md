# TODO / 路线图

> 这份文档记录**还没做**和**要评估**的事，不含已完成的历史。
> 约定：**先评估 → 再动手 → 攒够一批再 push + 更新 Release**（不要边做边发版）。

---

## A. 「更进一步」系列（Proxifier / gost 都没有的，我们的差异化）

| # | 事项 | 想法 | 估算 |
|---|---|---|---|
| A1 | **全链路解释补上出口 IP** | 现在「查一个 IP」能说到"命中第 N 条规则 → 链 proxy-a → 上游 A 可用 38ms"，缺最后一环：**出去之后是哪个公网 IP**。做法：让上游去回显地址（需要一个外部回显服务，可能要用户自己配一个 URL） | 1 天 |
| A2 | **场景一键切换** | 「只走已列出的内网」/「全部走隧道」/「维护模式（全直连）」三种模式一键切，各是一份规则集 | 1～2 天 |
| A3 | **业务系统视角** | 规则按"系统"分组（例如生产 / 测试 / 影像），界面按业务看"某系统走哪条链"，而不是平铺的 IP 列表 | 1～2 天 |
| A4 | **内网访问审计** | 谁（哪台机 / 哪个进程）什么时候访问了哪个内网目标，按天出记录，可导出 | 1～2 天 |
| A5 | ~~装机友好的分发~~ | ✅ 已做（2026-09-20）：设置页「导出客户装机包」= 程序+驱动+配置+启动脚本+装机说明+SHA256，不含任何运行期数据 | — |
| A6 | **DNS / UDP 泄漏防护** | 现在是靠 hosts + Clash 共存检测防 DNS 外泄。可进一步：检测"内网域名被系统 DNS 解析出去"、以及**同名地址的 UDP 绕过隧道**（我们只拦 TCP） | 1～2 天 |
| A7 | **「高级模式」界面分层** | 方向已定：简单模式只留"连内网 + 与 Clash 共存"，其余（端口条件、多上游策略、探测间隔、诊断细节）收进高级模式。**粒度待议**：是按页签分，还是每条能力一个"高级"折叠区 | 需先定设计 |
| A8 | **诊断包升级** | 今天的诊断包是"脱敏配置 + 日志 + 报告"。可加：与上一次配置的**差异对比**、上游探测历史趋势 | 0.5～1 天 |
| A9 | ~~规则模拟器~~ | ✅ 已做（2026-09-20）：路由规则页「模拟」支持一批 IP/网段，抽样 ≤16 点、支持 `#` 注释、按动作分组汇总 | — |
| **A10** | **上游竞速**（对照文档 G1） | 第一跳失败/超时后，并发试剩下的候选，把"半死→切到好上游"从 5s 压到 ~1 个 RTT | 0.5～1 天 |
| **A11** | **连接多路复用**（性能第一差距） | 在上游一条长连接上跑多条业务流，短连接不再每条都握手（现网每条多付 84～585 ms）。**需上游配合**（gost 的 pht/http2 是私有封装），与C6 一起评估 | 1～3 天 |
| **A12** | **环路检测**（G3） | 与 Clash 共存时最阴的坑：我们的流量被对方再送回来。用启发式（同目标高频 / 源=目标）在连接表与日志里报 | 1 天 |
| **A13** | **直连流量纳入统计**（G4） | 直连现在只统计出方向（回来的包不经过滤器）；要让"省了多少流量"能答 | 1～2 天 |
| **A14** | **按网络接口绑定规则**（G5，即原 C5） | 笔记本在公司走隧道、回家直连，不用手改配置 | 1 天 |
| **A15** | **哈希策略**（G6） | 多出口时同一台机器每次从不同出口出去 → 对方按 IP 做会话/白名单会飘 | 0.5 天 |
| **A16** | **配置加密 / 远程更新**（G7） | 装机包含明文口令（现在靠纪律）；客户多了要集中改配置 | 0.5 天 / 2～3 天 |

## B. 补齐 p / gost 的剩余能力

| # | 事项 | 说明 | 估算 |
|---|---|---|---|
| B1 | **上游协议 / 传输扩展** | 现在只有 socks4/5、socks5+tls、http(s)。gost 还有 WS/WSS、gRPC、QUIC、KCP、ss、ssh。**先确认上游将来会不会换实现**，不然容易白写 | 3～5 天 |
| B2 | **多跳串联**（A→B→C） | 我们的"链"是单跳多上游；gost 支持真正的多跳 | 1 天 |
| B3 | **UDP 处理** | 现在只拦 TCP（内网业务都是 TCP，所以没做）。要做需要上游支持 UDP associate | 2～3 天 |
| B4 | **IPv6** | 现在只做 IPv4；内网基本是 v4 | 2 天 |
| B5 | **规则目标跟随 hosts** | 比"通配域名"便宜的替代：hosts 里那几个域名解析成什么 IP，规则目标自动跟 | 1 天 |
| B6 | **应用 / 进程维度** | **明确取舍：默认不做**。理由是它带来的"每个新程序都要配一次"的成本，大于收益（我们按目标判定更省心）。除非出现"某程序绝对不许走隧道"的具体痛点 | 2～3 天（若要做） |

## C. 待评估（评估完再决定做不做）

| # | 事项 | 要回答的问题 |
|---|---|---|
| C1 | ~~缩短上游 dial 超时~~ | ✅ 已做：实测（`local/dialprobe`）后定 **单次 5s + 总预算 10s**；半死场景 10s→5s（真实上游握手 max 774ms，**6.5 倍余量**） |
| C2 | **每包用户态路径的吞吐上限** | 匹配到的流量每包都要过一次用户态（收包→改地址→校验和→发回）。理论上限多少？做法：本地假上游 + 本地大文件做合成压测（不碰客户内网） |
| C3 | **服务模式与界面共存** | 现在"服务与界面版二选一"（抢 WinDivert 驱动）。能否做成"界面只读地连服务"，让改配置不用先停服务？ |
| C4 | **日志 / 流量落盘与分级** | 细化日志级别（DEBUG/INFO/WARN/ERROR 分文件或分开关）、连接流量落盘（CSV） |
| C5 | ~~p v4 的"按网络接口绑定"~~ | ✅ 已并入 **A13**（对照 Proxifier：接口掉线自动切直连） |
| **C6** | **上游适配器抽象** | 见 `docs/能力对照-p与gost.md` 第四节风险 1：我们依赖 `socks5+tls`（gost 特有）。把每种上游做成独立模块 + 假上游回归测试，别往引擎里堆 gost 私有分支 | 0.5 天（下次动上游代码时顺手） |
| **C7** | **DNS 走代理 / 泄漏防护** | 先确认客户是否真有"内网域名只能内网解析"的场景（我们目标直接用 IP + hosts 托管） | 待评估 |
| **C8** | **抓包 dump** | 疑难问题抓原始流量比看日志有用；注意隐私与磁盘占用 | 1～2 天 |

## D. 工程 / 发布

| # | 事项 |
|---|---|
| D1 | **下一次 push 时更新 Release**：把这一批（规则动作、端口、阻断、连接列表、多上游故障转移、规则智能、巡检、体检、诊断包、服务模式…）发成 **v0.2.0**，附新包 + 新校验值 |
| D2 | 通读一遍 `docs/使用说明.md`：把「诊断」页、headless 模式、巡检设置、端口条件、多上游策略都写进去（部分已写，需补全并理顺） |
| D3 | 测试补强：诊断包内容断言、服务模式的自动化覆盖（安装/卸载需要管理员提权，目前只能手验） |
| D4 | 文档里的截图（`docs/screenshot.png`）还是早期界面，改完一轮后重拍 |

## E. 已知的"没验过 / 待验收"

| # | 事项 | 状态 |
|---|---|---|
| E1 | **Windows 服务的安装 / 启动 / 停止 / 卸载** | 代码与 CLI 都在（`-service-install` / `-service-uninstall` / `-service-state`），但**装/卸需要管理员提权**，开发环境验过；界面按钮请在有管理员权限时点一次确认 |
| E2 | 底部/长页面的布局（设置页四张卡、诊断页五张卡） | 截图只能看到首屏，滚动后的布局请人眼过一遍 |
| E3 | 连接页的字节统计口径 | 隧道连接统计双向；**直连只统计出方向**（回来的包不经内核过滤器），界面上有注记 |

## F. 对照文档里"明确不做"的项（已定，不再重复讨论）

按进程/应用维度、UDP 中继、多跳串联、TUN/TAP 组网、Prometheus metrics、域名/正则匹配、限速/配额、反向端口转发 ——
理由逐条写在 `docs/能力对照-p与gost.md` 第五节。
