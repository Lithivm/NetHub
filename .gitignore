*.exe
*.exe~
*.dll
*.sys
*.log
*.err
*.out
*.png
spike-archive/
build/

# 含上游凭据，绝不入库
config.yaml
config.yaml.*
!config.yaml.example

# Clash 绕过列表的实验备份（一次性）
.proxyoverride.bak

# install-task.ps1 生成的计划任务定义
NetHub-task.xml

# logo 源图（图标由 tools_mkicon.go 从它生成，要入库）
!assets/logo.jpg
!frontend/logo.png

# make-dist.ps1 的产物（二进制，含凭据，不入库）
dist/
dist-public/

# 内部运维脚本（写死了内网 IP / 客户域名，不进公开仓库；只留在本机用）
local/
